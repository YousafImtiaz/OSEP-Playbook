**Python:**

```
python3 -m http.server <port>
```

**Apache:**

> Place files in the webroot directory:

```
cd /var/www/html
```

> Start/stop server on port 80:

```
sudo systemctl start/stop apache2  
```
